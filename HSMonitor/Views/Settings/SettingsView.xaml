<UserControl
    x:Class="HSMonitor.Views.Settings.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:s="https://github.com/canton7/Stylet"
    xmlns:settings="clr-namespace:HSMonitor.ViewModels.Settings"
    xmlns:converters="clr-namespace:HSMonitor.Utils.Converters"
    xmlns:p="clr-namespace:HSMonitor.Properties"
    d:DataContext="{d:DesignInstance Type=settings:SettingsViewModel}"
    Style="{DynamicResource MaterialDesignRoot}"
    Background="{DynamicResource PrimaryHueLightBrush}"
    d:DesignWidth="{Binding Width}"
    d:DesignHeight="{Binding Height}"
    VerticalContentAlignment="Stretch"
    VerticalAlignment="Stretch"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style
            TargetType="{x:Type Button}"
            BasedOn="{StaticResource MaterialDesignFlatButton}">
            <Setter
                Property="Margin"
                Value="0, 4, 4, 4" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border
            x:Name="HeaderBorder"
            Grid.Row="0"
            Padding="4,6"
            Background="{DynamicResource PrimaryHueMidBrush}"
            TextElement.Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Logo -->
                <materialDesign:PackIcon
                    Grid.Column="0"
                    Width="24"
                    Height="24"
                    Kind="Settings"
                    Margin="4"
                    VerticalAlignment="Center" />

                <!-- Title -->
                <TextBlock
                    Grid.Column="1"
                    Margin="4,1,0,0"
                    VerticalAlignment="Center"
                    FontSize="16">
                    <Run
                        FontSize="19"
                        FontWeight="Medium"
                        Text="{Binding Title, Mode=OneWay}" />
                </TextBlock>

                <!-- Header actions: Reset / Cancel / Save -->
                <StackPanel
                    Grid.Column="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center">

                    <!-- Reset -->
                    <Button
                        Padding="6,2"
                        VerticalAlignment="Center"
                        Style="{DynamicResource MaterialDesignFlatDarkButton}"
                        Command="{s:Action Reset}"
                        ToolTip="{x:Static p:Resources.ResetButtonTooltip}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon
                                Width="18"
                                Height="18"
                                Margin="0,0,6,0"
                                Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                Kind="Restart" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                Text="Сброс" />
                        </StackPanel>
                    </Button>

                    <!-- Cancel -->
                    <Button
                        Padding="6,2"
                        VerticalAlignment="Center"
                        Style="{DynamicResource MaterialDesignFlatDarkButton}"
                        Command="{s:Action Cancel}"
                        ToolTip="{x:Static p:Resources.CancelButtonTooltip}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon
                                Width="18"
                                Height="18"
                                Margin="0,0,6,0"
                                Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                Kind="Close" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                Text="Отмена" />
                        </StackPanel>
                    </Button>

                    <!-- Save -->
                    <Button
                        Padding="6,2"
                        VerticalAlignment="Center"
                        Style="{DynamicResource MaterialDesignFlatDarkButton}"
                        Command="{s:Action Save}"
                        ToolTip="{x:Static p:Resources.SaveButtonTooltip}">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon
                                Width="18"
                                Height="18"
                                Margin="0,0,6,0"
                                Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                Kind="ContentSave" />
                            <TextBlock
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}"
                                Text="Сохранить" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content + bottom carousel -->
        <Grid Grid.Row="1">
            <!-- Active settings tab -->
            <materialDesign:Transitioner
                Grid.Row="0"
                ItemsSource="{Binding Tabs}"
                SelectedItem="{Binding ActiveTab, Mode=OneWay}">
                <materialDesign:Transitioner.Resources>
                    <Style TargetType="{x:Type materialDesign:TransitionerSlide}">
                        <Setter Property="BackwardWipe">
                            <Setter.Value>
                                <materialDesign:SlideWipe Direction="Left" Duration="0:0:0.2" />
                            </Setter.Value>
                        </Setter>
                        <Setter Property="ForwardWipe">
                            <Setter.Value>
                                <materialDesign:SlideWipe Direction="Right" Duration="0:0:0.2" />
                            </Setter.Value>
                        </Setter>
                    </Style>
                </materialDesign:Transitioner.Resources>

                <materialDesign:Transitioner.ItemTemplate>
                    <DataTemplate>
                        <Grid PreviewMouseWheel="OnPreviewMouseWheel">
                            <ScrollViewer
                                Padding="4"
                                Background="{DynamicResource PrimaryHueLightBrush}"
                                HorizontalScrollBarVisibility="Disabled"
                                VerticalScrollBarVisibility="Visible">
                                <StackPanel
                                    Orientation="Vertical">
                                    <ContentControl s:View.Model="{Binding}" />
                                    <Border
                                        Background="Transparent"
                                        Height="200px" />
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </DataTemplate>
                </materialDesign:Transitioner.ItemTemplate>
            </materialDesign:Transitioner>

            <!-- Settings tab carousel (bottom) -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="4*" />
                    <RowDefinition Height="1.5*" />
                </Grid.RowDefinitions>
                <Border
                    Grid.Row="1"
                    Background="Transparent"
                    SnapsToDevicePixels="True">

                    <Grid 
                        RenderOptions.BitmapScalingMode="LowQuality"
                        RenderOptions.EdgeMode="Aliased"
                        ClipToBounds="True">

                        <!-- Ваш контент поверх -->
                        <ScrollViewer
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollBarVisibility="Disabled"
                            CanContentScroll="True">

                            <ItemsControl ItemsSource="{Binding Tabs}" HorizontalAlignment="Center">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Button
                                            Margin="6,0"
                                            Width="64"
                                            Height="64"
                                            Padding="0"
                                            Command="{s:Action ActivateTab}"
                                            CommandParameter="{Binding}"
                                            ToolTip="{Binding Converter={x:Static converters:SettingsTabsToLocalizedNameConverter.Instance}}"
                                            HorizontalContentAlignment="Center"
                                            VerticalContentAlignment="Center">

                                            <Button.Style>
                                                <Style TargetType="{x:Type Button}">
                                                    <Setter Property="Background"
                                                            Value="{DynamicResource PrimaryHueLightBrush}" />
                                                    <Setter Property="Foreground"
                                                            Value="{DynamicResource PrimaryHueDarkBrush}" />
                                                    <Setter Property="BorderBrush"
                                                            Value="{DynamicResource PrimaryHueDarkBrush}" />
                                                    <Setter Property="BorderThickness" Value="1" />
                                                    <Setter Property="Cursor" Value="Hand" />
                                                    <Setter Property="SnapsToDevicePixels" Value="True" />
                                                    <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />

                                                    <!-- Template: все анимации тут, трансформы per-instance -->
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type Button}">
                                                                <Border x:Name="Bd"
                                                                        Background="{TemplateBinding Background}"
                                                                        BorderBrush="{TemplateBinding BorderBrush}"
                                                                        BorderThickness="{TemplateBinding BorderThickness}"
                                                                        CornerRadius="5"
                                                                        SnapsToDevicePixels="True"
                                                                        RenderTransformOrigin="0.5,0.5">

                                                                    <!-- 2 масштаба: selected + press -->
                                                                    <Border.RenderTransform>
                                                                        <TransformGroup>
                                                                            <ScaleTransform x:Name="SelectedScale"
                                                                                ScaleX="1"
                                                                                ScaleY="1" />
                                                                            <ScaleTransform x:Name="PressScale"
                                                                                ScaleX="1"
                                                                                ScaleY="1" />
                                                                        </TransformGroup>
                                                                    </Border.RenderTransform>

                                                                    <ContentPresenter
                                                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                        Margin="{TemplateBinding Padding}"
                                                                        TextElement.Foreground="{TemplateBinding Foreground}" />
                                                                </Border>

                                                                <ControlTemplate.Triggers>

                                                                    <!-- Hover для НЕ выбранной (Tag=False): инверсия -->
                                                                    <MultiTrigger>
                                                                        <MultiTrigger.Conditions>
                                                                            <Condition Property="IsMouseOver"
                                                                                Value="True" />
                                                                            <Condition Property="Tag" Value="False" />
                                                                        </MultiTrigger.Conditions>
                                                                        <Setter TargetName="Bd" Property="Background"
                                                                            Value="{DynamicResource PrimaryHueMidForegroundBrush}" />
                                                                        <Setter Property="Foreground"
                                                                            Value="{DynamicResource PrimaryHueMidBrush}" />
                                                                        <Setter Property="BorderBrush"
                                                                            Value="{DynamicResource PrimaryHueMidForegroundBrush}" />
                                                                    </MultiTrigger>

                                                                    <!-- Выбранная (Tag=True): инверсия + увеличение -->
                                                                    <DataTrigger Binding="{Binding IsSelected}"
                                                                        Value="True">
                                                                        <Setter TargetName="Bd" Property="Background"
                                                                            Value="{DynamicResource PrimaryHueMidBrush}" />
                                                                        <Setter Property="Foreground"
                                                                            Value="{DynamicResource PrimaryHueLightBrush}" />
                                                                        <Setter Property="BorderBrush"
                                                                            Value="{DynamicResource PrimaryHueLightBrush}" />
                                                                        <Setter Property="BorderThickness" Value="2" />

                                                                        <DataTrigger.EnterActions>
                                                                            <BeginStoryboard>
                                                                                <Storyboard>
                                                                                    <DoubleAnimation
                                                                                        Storyboard.TargetName="SelectedScale"
                                                                                        Storyboard.TargetProperty="ScaleX"
                                                                                        To="1.28" Duration="0:0:0.16" />
                                                                                    <DoubleAnimation
                                                                                        Storyboard.TargetName="SelectedScale"
                                                                                        Storyboard.TargetProperty="ScaleY"
                                                                                        To="1.28" Duration="0:0:0.16" />
                                                                                </Storyboard>
                                                                            </BeginStoryboard>
                                                                        </DataTrigger.EnterActions>

                                                                        <DataTrigger.ExitActions>
                                                                            <BeginStoryboard>
                                                                                <Storyboard>
                                                                                    <DoubleAnimation
                                                                                        Storyboard.TargetName="SelectedScale"
                                                                                        Storyboard.TargetProperty="ScaleX"
                                                                                        To="1" Duration="0:0:0.16" />
                                                                                    <DoubleAnimation
                                                                                        Storyboard.TargetName="SelectedScale"
                                                                                        Storyboard.TargetProperty="ScaleY"
                                                                                        To="1" Duration="0:0:0.16" />
                                                                                </Storyboard>
                                                                            </BeginStoryboard>
                                                                        </DataTrigger.ExitActions>
                                                                    </DataTrigger>

                                                                    <!-- Press: всегда, отдельный масштаб, не конфликтует с SelectedScale -->
                                                                    <Trigger Property="IsPressed" Value="True">
                                                                        <Trigger.EnterActions>
                                                                            <BeginStoryboard>
                                                                                <Storyboard>
                                                                                    <DoubleAnimation
                                                                                        Storyboard.TargetName="PressScale"
                                                                                        Storyboard.TargetProperty="ScaleX"
                                                                                        To="0.92" Duration="0:0:0.08" />
                                                                                    <DoubleAnimation
                                                                                        Storyboard.TargetName="PressScale"
                                                                                        Storyboard.TargetProperty="ScaleY"
                                                                                        To="0.92" Duration="0:0:0.08" />
                                                                                </Storyboard>
                                                                            </BeginStoryboard>
                                                                        </Trigger.EnterActions>

                                                                        <Trigger.ExitActions>
                                                                            <BeginStoryboard>
                                                                                <Storyboard>
                                                                                    <DoubleAnimation
                                                                                        Storyboard.TargetName="PressScale"
                                                                                        Storyboard.TargetProperty="ScaleX"
                                                                                        To="1" Duration="0:0:0.10" />
                                                                                    <DoubleAnimation
                                                                                        Storyboard.TargetName="PressScale"
                                                                                        Storyboard.TargetProperty="ScaleY"
                                                                                        To="1" Duration="0:0:0.10" />
                                                                                </Storyboard>
                                                                            </BeginStoryboard>
                                                                        </Trigger.ExitActions>
                                                                    </Trigger>

                                                                    <!-- Disabled -->
                                                                    <Trigger Property="IsEnabled" Value="False">
                                                                        <Setter TargetName="Bd" Property="Opacity"
                                                                            Value="0.45" />
                                                                    </Trigger>

                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>

                                            <!-- Контент: иконка + локализованный текст -->
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>

                                                <materialDesign:PackIcon
                                                    Grid.Row="0"
                                                    Width="22"
                                                    Height="22"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                                                    Kind="{Binding Converter={x:Static converters:SettingsTabsToIconConverter.Instance}}" />

                                                <TextBlock
                                                    Grid.Row="1"
                                                    Margin="0,2,0,0"
                                                    FontSize="10"
                                                    HorizontalAlignment="Center"
                                                    TextTrimming="CharacterEllipsis"
                                                    TextAlignment="Center"
                                                    Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"
                                                    Text="{Binding Converter={x:Static converters:SettingsTabsToLocalizedNameConverter.Instance}}" />
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</UserControl>